\documentclass[12pt,a4paper]{article}

% Text
\usepackage[english]{babel}

% Math
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}

% Algorithms
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

\begin{document}
	\begin{algorithm}
		\textbf{Input:} Sorted array, size of array and element to search for
		\\
		\textbf{Output:} Index of element in array if found, or -1
		\begin{algorithmic}
			\Procedure{Binary\_Search}{$array$, $n$, $element$}
				\State $ L \gets 0 $
				\State $ R \gets n-1 $
				\While{$ L \leq R $}
					\State $ m \gets \lfloor (L + R)/2 \rfloor $
					\If{$ array[m] == element $}
						\Return $ m $
					\ElsIf{$ array[m] < element $}
						\State $ l \gets m + 1 $
					\Else
						\State $ r \gets m - 1 $
					\EndIf
				\EndWhile
				\Return $ -1 $
			\EndProcedure
		\end{algorithmic}
	\end{algorithm}
\end{document}